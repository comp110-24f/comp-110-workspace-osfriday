"""While loop function writing"""

__author__ = "730566893"


def num_instances(search_str: str, inp_str: str) -> int:
    """Counting the non-overlapping occurences of search_str in inp_str."""
    count: int = 0
    idx: int = 0

    # wanna find the length of search_str then use that length to create the intervals of the index


# if inp_str [len(inp_str)-len(inp_str)] + inp_str [len(inp_str)-2] + inp_str [idx+2] + inp_str [idx+1]


def multiple_each_val_by_idx(x: int) -> str:
    """Multiplies each digit of an integer by their respective index positions"""
    